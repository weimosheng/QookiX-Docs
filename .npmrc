# 关闭 peer 依赖自动补装，使 lockfile 在 npm 10 / 12 下解析结果一致。
#
# 背景：vitepress 依赖的 @docsearch/react@3.8.2 声明了
#   react / react-dom / @types/react : ">= 16.8.0 < 19.0.0"（均为 optional peer）
# 而项目使用 React 19。npm 10.x 会为这些 optional peer 补装一份 React 18
# 到 node_modules/@docsearch/js/node_modules/ 下，npm 11+ 则直接跳过冲突的 optional peer。
# 本仓库的 package-lock.json 由 npm 12 生成（不含 React 18 节点），
# 因此 npm 10 执行 `npm ci` 时会认为 lockfile 与依赖树不同步并报：
#   Missing: react@18.3.1 / react-dom@18.3.1 / @types/react@18.3.31 ... from lock file
legacy-peer-deps=true
